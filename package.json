{
  "name": "mi-portafolio",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run build && gh-pages -d dist",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.7.4"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
